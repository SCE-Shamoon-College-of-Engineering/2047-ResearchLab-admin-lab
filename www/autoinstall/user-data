#cloud-config
autoinstall:
  version: 1
  locale: en_US
  keyboard: {layout: us, variant: ''}
  identity:
    hostname: student-pc
    username: sce
    # Сгенерируй хеш: mkpasswd -m sha-512
    password: "$6$rounds=4096$REPLACE$THIS$WITH$HASH"
  ssh:
    install-server: true
    authorized-keys:
      - "ssh-ed25519 AAAA... ТВОЙ_SSH_KEY ..."
  storage:
    layout: { name: lvm }
  packages:
    - curl
    - git
    - wget
    - ca-certificates
  late-commands:
    - curtin in-target -- systemctl enable ssh

